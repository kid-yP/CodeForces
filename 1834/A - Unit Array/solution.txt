t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
 
    neg = a.count(-1)
    pos = n - neg
 
    ops = 0
 
    while True:
        if neg % 2 == 0 and pos >= neg:
            break
 
        neg -= 1
        pos += 1
        ops += 1
 
    print(ops)