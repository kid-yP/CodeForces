t = int(input())for _ in range(t):    n = int(input())        alice_white = alice_black = 0    bob_white = bob_black = 0        i = 1    position = 1        while n > 0:        cards = min(i, n)                if position % 2 == 1:            white = (cards + 1) // 2            black = cards // 2        else:            black = (cards + 1) // 2            white = cards // 2                if i % 4 == 0 or i % 4 == 1:            alice_white += white            alice_black += black        else:            bob_white += white            bob_black += black                n -= cards        position += cards        i += 1        print(alice_white, alice_black, bob_white, bob_black)