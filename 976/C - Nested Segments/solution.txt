import sysinput = sys.stdin.readline n = int(input()) segments = []for i in range(n):    l, r = map(int, input().split())    segments.append((l, r, i + 1)) segments.sort(key=lambda x: (x[0], -x[1])) max_r = -1max_index = -1 for l, r, idx in segments:    if r <= max_r:        print(idx, max_index)        break    if r > max_r:        max_r = r        max_index = idxelse:    print(-1, -1)