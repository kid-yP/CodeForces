n = int(input())x = list(map(int, input().split()))v = list(map(int, input().split())) def can(t):    left = -1e18    right = 1e18        for i in range(n):        l = x[i] - v[i] * t        r = x[i] + v[i] * t                left = max(left, l)        right = min(right, r)        return left <= right low = 0.0high = 1e9  for _ in range(100):    mid = (low + high) / 2        if can(mid):        high = mid    else:        low = mid print(high)