t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
 
    max_len = 0
    current = 0
 
    for x in a:
        if x == 0:
            current += 1
            max_len = max(max_len, current)
        else:
            current = 0
 
    print(max_len)