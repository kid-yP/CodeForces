t = int(input())for _ in range(t):    n, x = map(int, input().split())    stations = list(map(int, input().split()))        max_dist = stations[0]        for i in range(1, n):        max_dist = max(max_dist, stations[i] - stations[i-1])        max_dist = max(max_dist, 2 * (x - stations[-1]))        print(max_dist)