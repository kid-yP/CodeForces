t = int(input())for _ in range(t):    n, l, r = map(int, input().split())    arr = list(map(int, input().split()))        rounds = 0    left = 0    curr_sum = 0        for right in range(n):        curr_sum += arr[right]                while left <= right and curr_sum > r:            curr_sum -= arr[left]            left += 1                if l <= curr_sum <= r:            rounds += 1            curr_sum = 0            left = right + 1        print(rounds)