import sysinput = sys.stdin.readline n = int(input())m = list(map(int, input().split())) left = [0] * nright = [0] * n stack = [] for i in range(n):    while stack and m[stack[-1]] > m[i]:        stack.pop()     if stack:        j = stack[-1]        left[i] = left[j] + m[i] * (i - j)    else:        left[i] = m[i] * (i + 1)     stack.append(i) stack.clear() for i in range(n - 1, -1, -1):    while stack and m[stack[-1]] > m[i]:        stack.pop()     if stack:        j = stack[-1]        right[i] = right[j] + m[i] * (j - i)    else:        right[i] = m[i] * (n - i)     stack.append(i) best = 0peak = 0 for i in range(n):    total = left[i] + right[i] - m[i]    if total > best:        best = total        peak = i a = [0] * na[peak] = m[peak] for i in range(peak - 1, -1, -1):    a[i] = min(m[i], a[i + 1]) for i in range(peak + 1, n):    a[i] = min(m[i], a[i - 1]) print(*a)