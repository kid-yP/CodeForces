t = int(input())for _ in range(t):    dist = list(map(int, input().split()))    count = 0     t = dist[0]    for i in range(1, len(dist)):        if t < dist[i]:            count += 1        else:            continue    print(count)